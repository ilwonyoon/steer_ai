# user-specific Xcode state
**/xcuserdata/
**/*.xcworkspace/xcuserdata/
xcuserdata/

# build artifacts
DerivedData/
build/

# generated by xcodegen — keep project.yml as the source of truth.
# We DO check in Steer.xcodeproj because the spike uses it directly,
# and contributors who don't have xcodegen still get a working project.

# Claude Code per-project state
.claude/
